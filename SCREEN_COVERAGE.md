# تقرير تغطية الشاشات — مقارنة وثيقة الـ design مع الموقع

**المرجع:** `design md` (10 أقسام مفصّلة + قائمة رئيسية)
**الكود:** `src/screens/*.tsx` (التوجيه عبر `sub` في كل وحدة)
**آخر تحديث:** 2026-08-01 (اكتملت جميع النواقص)

---

## 1. مدير الحسابات (Accounts) — ✅ كاملة (14/14)

| الشاشة في الـ design | الحالة | حالة التنفيذ |
|---|---|---|
| ➕ إضافة حساب جديد | موجودة (`add`) | وظيفية (OTP/2FA) |
| 📂 استيراد الجلسات | موجودة (`import`) | وظيفية |
| 📋 عرض الحسابات | موجودة (`list`) | وظيفية |
| 🔍 بطاقة تفاصيل الحساب | موجودة (`detail/:id`) | وظيفية |
| ✅ التحقق من الصحة | موجودة (`validate`) | وظيفية |
| 👥 مجموعات الحسابات (Pools) | موجودة (`pools`) | وظيفية |
| 🔥 تهيئة (تسخين) | موجودة (`warmup`) | وظيفية |
| 📊 درجة صحة الحسابات | موجودة (`health`) | وظيفية |
| ⚙️ إعدادات فردية | موجودة (`settings-ind`) | وظيفية |
| 🖼️ إدارة ملفات الشخصية | موجودة (`profile`) | وظيفية |
| 📋 سجل نشاط الحسابات | موجودة (`activity`) | وظيفية |
| 🔒 أمان الحسابات | موجودة (`security`) | وظيفية |
| 💾 تصدير/استيراد | موجودة (`export`/`import`) | وظيفية |
| 🗑️ إدارة غير النشطة | موجودة (`remove`/`auto-remove`) | وظيفية |

## 2. تجميع الأعضاء (Gather) — ✅ كاملة (12/12)

public, private, chat, visible, post, bulk, advanced, cleaner, merge, files, templates, stats — كلها موجودة وموجّهة.

## 3. إضافة الأعضاء (Add) — ✅ كاملة (8/8)

csv, manual, smart, multi, resume, blacklist, logs, defaults — كلها موجودة.

## 4. نظام التدوير (Rotation) — ✅ كاملة (12/12)

settings, table, edit, usage, reset + **المضاف حديثاً**: smart (التدوير الذكي), schedule (جدولة), profiles (سيناريوهات), analytics (تحليلات), live (مراقب حي), exclusion (قواعد الاستبعاد), notifications (إشعارات).

## 5. مدير البروكسي (Proxy) — ✅ كاملة (13/13)

add, import, list, validate, assign-manual, assign-auto, remove-dead + **المضاف حديثاً**: detail (بطاقة تفاصيل), pools (مجموعات), replace-dead (استبدال الميتة), stats (إحصائيات), export (تصدير), notifications (إشعارات), general (إعدادات عامة).

## 6. الإعدادات (Settings) — ✅ كاملة (14 قسم فرعي)

حوّلت من شاشة واحدة إلى وحدة تضم **14 شاشة فرعية**: api, limits, storage, notifications, security, language, database, logging, scheduling, access, backup, performance, sysinfo, reset.

## 7. التقارير والسجلات (Reports) — ✅ كاملة (13/13)

today, week, gather-log, add-log, errors, export + **المضاف حديثاً**: dashboard (لوحة حية), monthly (تقرير شهري), massdm-log (سجل الرسائل), accounts (تقارير الحسابات), analytics (تحليلات متقدمة), leaderboard (لوحة الترتيب), manage (إدارة السجلات).

## 8. أدوات الأمان (Security) — ✅ كاملة (13/13)

blacklist, limits, verify, clean, monitor, backup + **المضاف حديثاً**: audit (فحص أمان شامل), sessions (الأجهزة المتصلة), 2fa, encryption (تشفير الجلسات), emergency (استجابة للطوارئ), security-notifications (تنبيهات الأمان), security-reports (تقارير الأمان).

## 9. الرسائل الجماعية (Mass DM) — ✅ كاملة (7/7)

new, list, resume, blacklist, templates, stats, settings — كلها موجودة.

## 10. حملات رسائل القروبات (Group Campaigns) — ✅ كاملة (8/8)

new, list, resume, groups, templates, schedule, stats, settings — كلها موجودة.

---

## ملخص

| القسم | في الـ design | في الموقع | النتيجة |
|---|---|---|---|
| مدير الحسابات | 14 | 14 | ✅ كاملة |
| تجميع الأعضاء | 12 | 12 | ✅ كاملة |
| إضافة الأعضاء | 8 | 8 | ✅ كاملة |
| نظام التدوير | 12 | 12 | ✅ كاملة |
| مدير البروكسي | 13 | 13 | ✅ كاملة |
| الإعدادات | 14 | 14 | ✅ كاملة |
| التقارير والسجلات | 13 | 13 | ✅ كاملة |
| أدوات الأمان | 13 | 13 | ✅ كاملة |
| الرسائل الجماعية | 7 | 7 | ✅ كاملة |
| حملات القروبات | 8 | 8 | ✅ كاملة |

**النتيجة: جميع الشاشات في كل الأقسام أصبحت موجودة وكاملة.** ✅

> ملاحظات تقنية:
> - تم تحديث `src/App.tsx` لتوجيه شاشات الإعدادات الفرعية عبر `SettingsScreen`.
> - الشاشات المضافة تتبع أسلوب المشروع (mock + toast) ونفس مكوّنات `ui.tsx`.
> - بعض الشاشات المضافة تعتمد على mock بيانات حتى يُربط الباك-إند بالكامل.

---

## تحديث — إكمال تفاصيل الأقسام (2026-08-01)

تمت مراجعة `design md` بدقة وأُكملت التفاصيل الناقصة داخل الشاشات (بدون حذف أي شاشة أو إجراء):

### مدير الحسابات
- **استيراد الجلسات**: أصبح يدعم 5 طرق كاملة (مجلد / ملف واحد / ملف نصي / String Session / ZIP مع كلمة مرور) مع فحص ونتائج.
- **تصدير الجلسات**: تنزيل ZIP حقيقي من endpoint جديد `/uploads/sessions/backup` + Metadata CSV.
- **درجة صحة الحسابات**: أضيف شرح نظام الدرجات + عوامل الحساب + فلتر الضعيفة + زر تسخين.
- **عرض الحسابات**: أضيف إجراءات جماعية على المحدد (تسخين / تحقق / تعيين بروكسي).

### إضافة الأعضاء
- **شاشة التنفيذ**: أضيف خيارات الإيقاف المؤقت الكاملة (استئناف الآن / بعد فترة / تعديل إعدادات ثم استئناف) + محاكاة FloodWait وحظر.
- **استئناف عملية**: أضيف "استئناف بنفس الإعدادات" و"بإعدادات معدّلة".
- **CSV تصفح مسار آخر**: رفع ملف CSV حقيقي.
- **القائمة السوداء**: استيراد CSV/ملف + تصدير حقيقي.

### التقارير والسجلات
- **سجل التجميع**: بحث + فلاتر تاريخ + تفاصيل + تصدير.
- **سجل الإضافة**: فلاتر كيان + تفاصيل + تصدير.
- **سجل الأخطاء**: فلاتر مستوى/نوع + تفاصيل + تصدير.
- **تصدير التقارير**: فترة + تضمين + صيغ + جدولة تلقائية + إرسال لـ.
- **تقرير اليوم**: أضيف قسم الأداء (أفضل حساب/بروكسي) + مقارنة بالأمس.

### الأمان
- **الحدود الذكية**: قواعد بحدود مخصصة + مستوى مخصص + جدول موصى + عرض التأثير + حفظ.
- **مراقب الحظر**: تفعيل/إيقاف + فترة فحص + جدول حي + إجراءات عند الحظر + إشعارات.
- **تنظيف الحسابات**: اختيار نطاق + عمليات تنظيف تفصيلية + تقدم حي + إيقاف مؤقت.

### الإعدادات
- **قاعدة البيانات**: ضغط VACUUM + نسخ/تصدير احتياطي + مسح سجلات (endpoints جديدة `/system/database/*`).
- **معلومات النظام**: جلب حي من `/health/system`.

### الحملات / الرسائل الجماعية
- **تصميم الرسالة**: معاينة + عدد أحرف + حفظ كقالب + إرسال تجريبي.
- **جدولة الحملات**: معالج إنشاء (مرة واحدة / يومي / أيام / كل X ساعات) + إيقاف/استئناف + حذف.

### الباك-إند
- endpoint جديد `/system` (معلومات النظام، نسخ/ضغط/تنظيف قاعدة البيانات) + `/uploads/sessions/backup` (ZIP).
- جميع الـ endpoints الجديدة تعمل (34/34 check).
